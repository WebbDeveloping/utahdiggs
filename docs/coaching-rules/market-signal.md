# Market signal

## V1 scope — **out** (archive)

Do **not** build the Weekly Weak / On Track / Strong widget or rating dots. Overview uses the **price-health verdict** + core stats instead ([canonical-thresholds.md](./canonical-thresholds.md)).

This file preserves prototype thresholds for historical reference only.

## Purpose (prototype)

Rate last-week views / saves / showings / offers and emit a coaching sentence.

## Why skipped

Duplicates the Overview verdict; multiple rating systems confused sellers.

## Historical thresholds

See git history / prototype `updateMarketSignal()` if ever revisited. Not part of v1 `coaching-rules.ts`.
