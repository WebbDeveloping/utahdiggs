# Your Market

## V1 scope — **slim**

- **Keep:** city snapshot numbers (inventory, sold, avg DOM, avg prices, etc. as already shown)
- **Optional later:** one listing-vs-city compare (DOM / price / $/sqft) using **same** DOM bands as Overview
- **Do not build:** market status badge, Blair’s Take, color legend, per-card MoM tip rainbow, absorption zone UI, list-vs-sold coaching bar, city price-reduction insight banner + CTA

Overview gets a **small teaser** (2–3 numbers + link here). See [overview-and-pages.md](./overview-and-pages.md).

## Purpose

City context so sellers understand the environment — not a second coaching engine.

## Rebuild status

- Numbers page exists (`AccountYourMarketCards`)
- Overview teaser links here (`OverviewMarketTeaser`)
- Coaching layers from prototype remain unimplemented (intentionally for v1)
- Product TODO marked done for v1 slim scope

## Historical

Full prototype rule tables (borders, absorption, `_mktStatus`, etc.) lived here previously — superseded by slim scope. See git history if needed.
