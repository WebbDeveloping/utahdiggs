Introduction
The Anna — Seller Portal System API provides an easy way to integrate your Anna — Seller Portal System data in Airtable with any external system. The API closely follows REST semantics, uses JSON to encode objects, and relies on standard HTTP codes to signal operation outcomes.

The API documentation below is specifically generated for your base. We recommend that you use the graphical Airtable interface to add a few records of example data for each table. These records will be displayed in the documentation examples generated below.

To view documentation for all available endpoints, as well as documentation that has not been generated specific to your base, please visit here.

The ID of this base is appNh6k6ZP0c2OtbQ.

Please note: if you make changes to a field (column) name or type, the API interface for those fields will change correspondingly. Therefore, please make sure to update your API implementation accordingly whenever you make changes to your Airtable schema from the graphical interface.

Official API client:

JavaScript: airtable.js (Node.js + browser)
Community-built API clients:

Ruby: airrecord
.NET: airtable.net
Python 3: pyairtable
Python 2/3: airtable.py
Metadata
This API gives you the ability to list all of your bases, tables, fields, and views. For more, see the API reference documentation for List bases and Get base schema.

Rate Limits
The API is limited to 5 requests per second per base. If you exceed this rate, you will receive a 429 status code and will need to wait 30 seconds before subsequent requests will succeed.

The official JavaScript client has built-in retry logic.

If you anticipate a higher read volume, we recommend using a caching proxy. This rate limit is the same for all plans and increased limits are not currently available.

Authentication
Airtable uses simple token-based authentication. For personal development, we recommend using personal access tokens, which can be created at /create/tokens. To learn more about other authentication methods like OAuth, please visit our developer documentation.

Using Airtable.js in the browser is strongly discouraged, since that will expose your API key to everyone who has access to that web page.

To configure Airtable.js either store your secret API token (e.g. personal access token) in the AIRTABLE_API_KEY environment variable or pass it directly to the client library. Note that environment variables are not available when using Airtable.js in the browser.

All API requests must be authenticated and made over HTTPS.

 
Example using environment variable
# Shell:
$ export AIRTABLE_API_KEY=YOUR_SECRET_API_TOKEN

# Node:
const base = require('airtable').base('appNh6k6ZP0c2OtbQ');
Example using custom configuration
var Airtable = require('airtable');
Airtable.configure({
    endpointUrl: 'https://api.airtable.com',
    apiKey: 'YOUR_SECRET_API_TOKEN'
});
var base = Airtable.base('appNh6k6ZP0c2OtbQ');
Listings Table
The id for Listings is tbltohB45hwmmGjE3. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Listings table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Addressfld7WEH1cCmWB6b90Text
string
A single line of text.
 
Example values
"8278 S RESACA DR #L1, Sandy, UT 84070"

"123 Test Lane, Salt Lake City, UT 84101"

"330 E 7635 S, Sandy, UT 84047"

"340 E 7635 S, Sandy, UT 84047"

"350 E 7635 S, Sandy, UT 84047"

CityfldI97zzqyjdH46YLText
string
A single line of text.
 
Example values
"Sandy"

"Sandy"

"Sandy"

"Sandy"

"Sandy"

StatefldiLLv9dyPaPEWQIText
string
A single line of text.
 
Example values
"UT"

"UT"

"UT"

"UT"

"UT"

Zipfld3hQ9mRUMlP4OHVText
string
A single line of text.
 
Example values
"84070"

"84047"

"84047"

"84047"

"84047"

List PricefldxDrSkVmkFCIeTICurrency
number
Currency value. This field only allows positive numbers.
 
Example values
309990

529700

539900

499900

1074000

Statusfld7RFzyUwVOMlxk4
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Active",
    "Under Contract",
    "Closed",
    "Pending",
    "Cancelled"
]
List Datefld6To85i8oPmYnWz
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-05-01"

"2026-02-24"

"2026-04-21"

"2026-04-29"

"2026-02-25"

MLS NumberfldRvIW94gZeoq1KnText
string
A single line of text.
 
Example values
"2138751"

"2139122"

"2152174"

"2154223"

"2139383"

Portal SlugfldYjSRVF5yuCiH3eText
string
A single line of text.
 
Example values
"8278-s-resaca-dr-l1-sandy-ut-84070-kvmx"

"123-test-lane-salt-lake-city-ut-84101-rjia"

"330-e-7635-s-sandy-ut-84047-4vqo"

"340-e-7635-s-sandy-ut-84047-5ov1"

"350-e-7635-s-sandy-ut-84047-vl75"

Portal URLfldlBghpBvFbNVxE7URL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://portal.utahdigs.com/8278-s-resaca-dr-l1-sandy-ut-84070-kvmx"

"https://portal.utahdigs.com/123-test-lane-salt-lake-city-ut-84101-rjia"

"https://portal.utahdigs.com/330-e-7635-s-sandy-ut-84047-4vqo"

"https://portal.utahdigs.com/340-e-7635-s-sandy-ut-84047-5ov1"

"https://portal.utahdigs.com/350-e-7635-s-sandy-ut-84047-vl75"

Passcodefld1GnKtltIN0NOGWText
string
A single line of text.
 
Example values
"5057"

"5057"

"0122"

"0122"

"0122"

Lofty Transaction IDfld8zTMAfZipB8BUTText
string
A single line of text.
 
Example values
"1775045"

Notes from Blairfld6XnjP9je5pjWinLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example value
"Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque vel accumsan orci. Fusce interdum ipsum nec neque iaculis congue. Vivamus bibendum quam at sollicitudin vestibulum. Nunc suscipit nisl sit amet enim hendrerit pellentesque. Curabitur non fermentum orci."
Sellersfld6m2EVL7TqdNyRz
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Sellers table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Weekly StatsfldpoqFoFN5URUNPo
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Weekly Stats table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Showingsflds4Y6CPi3Gto9yW
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Showings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Market DatafldYmKe23QWn7tees
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Market Data table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Seller RequestsfldFGVrqxFh1mCQCj
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Seller Requests table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

BedsfldHmAHJ691sIMIJqNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
3

3

Bathsfld5L68MgGY1Dn3QFNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
2.0

2.5

SqftfldltANWzBoqLmb3tNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
1244

1830

Blair NotefldjJQ115o3QqBXiHLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example values
"A showing this week, and online traffic stays strong — nearly a thousand portal views in the last month. The listing is doing its job pulling people i..."

"One showing Saturday. Honest feedback — buyer liked the condition and thought the price was fair, but a townhouse with stairs wasn't their layout. Tha..."

"Quiet week — no showings. When a unit goes still it's almost always price relative to what else buyers are seeing, since condition and access here are..."

"One showing this week, which puts us at nine total on this unit. Traffic's been steady but we're still not converting to offers, and after this many l..."

"No traffic this week. Past the four-month mark, and at this price the homes that move feel like the clear best value in their tier. Condition and acce..."

Blair Note DatefldKJzM5cZdq9EuG8
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-22"

"2026-06-22"

"2026-06-22"

"2026-06-22"

"2026-06-22"

Latest ViewsfldwgBBBqLDW9J5xuNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
967

284

346

721

321

Latest SavesfldGODXbdCO0u1yZONumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
9

0

0

0

0

Latest Showingsfldu8tfNGZfaSYZ2hNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
1

1

0

1

0

Active Offersfldwm9iZMcyM3ocJ7Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

OffersfldaF5YMlgmA8HPhD
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Offers table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Active Offers CountfldJKTPUGHSa9dvC1
Rollup
number or string
Computed value: SUM(IF(values = "Pending Review", 1, 0)) for Status in Offers.
 
Example values
0

0

0

0

0

Seller Emailfldbk52Y4dFOctI3YEmail
string
A valid email address.
 
Example values
"blairjallen@gmail.com"

"blairjallen@gmail.com"

"blair.jallen@gmail.com"

"blair.jallen@gmail.com"

"blair.jallen@gmail.com"

Offer Form URLfldVwXQV3duLjQUsqFormula
number, string, array of numbers or strings
Computed value: "https://form.jotform.com/261706179400050?listing="&ENCODE_URL_COMPONENT({Address})&"&seller_email="&ENCODE_URL_COMPONENT({Seller Email}).
 
Example values
"\"https://form.jotform.com/261706179400050?listing=8278%20S%20RESACA%20DR%20%23L1%2C%20Sandy%2C%20UT%2084070&seller_email=blairjallen%40gmail.com\""

"\"https://form.jotform.com/261706179400050?listing=123%20Test%20Lane%2C%20Salt%20Lake%20City%2C%20UT%2084101&seller_email=blairjallen%40gmail.com\""

"\"https://form.jotform.com/261706179400050?listing=330%20E%207635%20S%2C%20Sandy%2C%20UT%2084047&seller_email=blair.jallen%40gmail.com\""

"\"https://form.jotform.com/261706179400050?listing=340%20E%207635%20S%2C%20Sandy%2C%20UT%2084047&seller_email=blair.jallen%40gmail.com\""

"\"https://form.jotform.com/261706179400050?listing=350%20E%207635%20S%2C%20Sandy%2C%20UT%2084047&seller_email=blair.jallen%40gmail.com\""

Days on MarketfldZUuNMocjVILiijFormula
number, string, array of numbers or strings
Computed value: DATETIME_DIFF(TODAY(), {List Date}, 'days').
 
Example values
56

{
    "specialValue": "NaN"
}

122

66

58

Status LabelfldCN7wmFY2EutvU1Formula
number, string, array of numbers or strings
Computed value: {Status}.
 
Example values
"\"Active\""

"\"Active\""

"\"Active\""

"\"Active\""

"\"Active\""

Seller NamefldZYvIiQ6JE5lklQText
string
A single line of text.
 
Example values
"Blair Allen"

"Blair Allen"

"Brandon Fry"

"Brandon Fry"

"Brandon Fry"

Seller PhonefldlibYKncOA9K4mx
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"(801) 337-5057"

"(801) 337-5057"

"801-718-1331"

"801-718-1331"

"801-718-1331"

Lofty Contact IDfldHflDufn8OC4YTsText
string
A single line of text.
 
Example values
"foo", "bar"

Portal Email Sentfld1M4ax47FPLEuiQ
Checkbox
boolean
This field is "true" when checked and otherwise empty.
 
Example value
true

Co-Seller 1 EmailfldDQIUYbU4XNbUKgEmail
string
A valid email address.
 
Example values
"b.lairjallen@gmail.com"

"b.lairjallen@gmail.com"

"b.lairjallen@gmail.com"

"b.lairjallen@gmail.com"

"b.lairjallen@gmail.com"

Co-Seller 2 Emailfldr9H6N3LkS7XkyrEmail
string
A valid email address.
 
Example values
"support@example.com", "sales@example.com"

Co-Seller 3 Emailfld9QfdW16s7eW3wuEmail
string
A valid email address.
 
Example values
"support@example.com", "sales@example.com"

Price Reduction DatefldndfP52rH9Owo7R
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example value
"2014-09-05"

Zillow URLfldMvR4mSl7NiVieJURL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://www.zillow.com/homes/330-E-7635-S-Midvale--UT-84047/12880839_zpid/"

"https://www.zillow.com/homes/340-E-7635-S-Midvale--UT-84047/461852167_zpid/"

"https://www.zillow.com/homes/350-E-7635-S-Midvale--UT-84047/2080820057_zpid/"

"https://www.zillow.com/homes/354-E-7635-S-Midvale--UT-84047/305066055_zpid/"

"https://www.zillow.com/homes/7613-S-300-E-Midvale--UT-84047/12880830_zpid/"

Zillow SavesfldDnoJxWgNOys6n7Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Zillow ViewsflddFvR0RQL7EWBgNNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Price Reduction Amountfld8L5dls8WXJz9AlCurrency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

Price Reduction CountfldJEA5WQPIhFL0yPNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Zillow Saves Running TotalfldvYV1NOO2Mo5MzDNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Portfolio GroupfldhQm6LJodfJYSmSText
string
A single line of text.
 
Example values
"brandon-fry"

"brandon-fry"

"brandon-fry"

"brandon-fry"

"brandon-fry"

Portfolio PasscodeflduJORajveyIIVI0Text
string
A single line of text.
 
Example values
"0122"

"0122"

"0122"

"0122"

"0122"

Market Avg DOMfldrbJDjvdXUC8JxUNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
45

45

45

45

45

Portfolio TokenfldqCPGrzJp9bW6AOText
string
A single line of text.
 
Example values
"brandon-fry"

"brandon-fry"

"brandon-fry"

"brandon-fry"

"brandon-fry"

Escrow OfficerfldtWO130AnfIk5zh
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Closing Team table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Transaction Coordinatorfldqkrx1euXDXkCWL
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Closing Team table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

DocumentsfldqM6F9NU7oT3dHr
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Documents table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Open URLfld14DzIDgepW5q1q
Button
button object
Object providing details about the button configuration.

labelstring
button label
urlstring
for "Open URL" actions, the computed url value
 
Example value
{
    "label": "Portal Login",
    "url": "https://airtable.com/tbltohB45hwmmGjE3/reckLQ0OlLD3B8sxl?blocks=blidAHLUkrP73YLuV"
}

List Listings records
To list records in Listings, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Address and City, pass in:

fields: ["Address", "City"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fld7WEH1cCmWB6b90", "fldI97zzqyjdH46YL"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Address isn't empty, pass in NOT({Address} = '') as a parameter like this:

filterByFormula: "NOT({Address} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Address in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Address
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Address in descending order, pass in:

[{field: "Address", direction: "desc"}]
view
string
optional
The name or ID of a view in the Listings table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Listings').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Address'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved 8278 S RESACA DR #L1, Sandy, UT 84070
Retrieved 123 Test Lane, Salt Lake City, UT 84101
Retrieved 330 E 7635 S, Sandy, UT 84047
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Listings').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Address'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Listings').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Listings record
To retrieve an existing record in Listings table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Listings').find('reckLQ0OlLD3B8sxl', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved reckLQ0OlLD3B8sxl
Create Listings records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

Values for Active Offers Count, Offer Form URL, Days on Market, Status Label and Open URL are automatically computed by Airtable and cannot be directly created.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Listings').create([
  {
    "fields": {
      "Address": "8278 S RESACA DR #L1, Sandy, UT 84070",
      "City": "Sandy",
      "State": "UT",
      "Zip": "84070",
      "List Price": 309990,
      "Status": "Active",
      "List Date": "2026-05-01",
      "MLS Number": "2138751",
      "Portal Slug": "8278-s-resaca-dr-l1-sandy-ut-84070-kvmx",
      "Portal URL": "https://portal.utahdigs.com/8278-s-resaca-dr-l1-sandy-ut-84070-kvmx",
      "Passcode": "5057",
      "Lofty Transaction ID": "1775045",
      "Sellers": [
        "recnyg3dtFgHxnxad"
      ],
      "Weekly Stats": [
        "rec0csrajaNvKYmcd",
        "rec3EUq6tIDyFp1Cr",
        "recSLJusuGrWE1nyw",
        "recReMunW0QEl0cre"
      ],
      "Showings": [
        "recsTNxFP1KzeWTln",
        "recQyAL1RnwsSqob9",
        "recbSPpH1egIWj5ML"
      ],
      "Beds": 3,
      "Baths": 2,
      "Sqft": 1244,
      "Blair Note": "A showing this week, and online traffic stays strong — nearly a thousand portal views in the last month. The listing is doing its job pulling people i...",
      "Blair Note Date": "2026-06-22",
      "Latest Views": 967,
      "Latest Saves": 9,
      "Latest Showings": 1,
      "Active Offers": 0,
      "Offers": [
        "recnYuAl33QJIUt4q",
        "rec6eru7ntdBRJE05",
        "recaBseMhjqjEa34v",
        "recZBCkuLKXNTesPv"
      ],
      "Seller Email": "blairjallen@gmail.com",
      "Seller Name": "Blair Allen",
      "Seller Phone": "(801) 337-5057",
      "Portal Email Sent": true,
      "Market Avg DOM": 45,
      "Escrow Officer": [
        "rec39Hafyn2Y9Jlla"
      ],
      "Transaction Coordinator": [
        "recgsIboMs68c7mg2"
      ]
    }
  },
  {
    "fields": {
      "Address": "123 Test Lane, Salt Lake City, UT 84101",
      "Portal Slug": "123-test-lane-salt-lake-city-ut-84101-rjia",
      "Portal URL": "https://portal.utahdigs.com/123-test-lane-salt-lake-city-ut-84101-rjia",
      "Passcode": "5057",
      "Seller Email": "blairjallen@gmail.com",
      "Seller Name": "Blair Allen",
      "Seller Phone": "(801) 337-5057",
      "Portal Email Sent": true
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
reckLQ0OlLD3B8sxl
reclBPWmG8sptzCbn
Update Listings records
To update Listings records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Listings records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Sellers, Weekly Stats, Showings, Market Data, Seller Requests, Offers, Escrow Officer, Transaction Coordinator and Documents, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Notes from Blair and Blair Note may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

Values for Active Offers Count, Offer Form URL, Days on Market, Status Label and Open URL are automatically computed by Airtable and cannot be directly updated. You cannot clear these, even with a replace call.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Listings').update([
  {
    "id": "reckLQ0OlLD3B8sxl",
    "fields": {
      "Address": "8278 S RESACA DR #L1, Sandy, UT 84070",
      "City": "Sandy",
      "State": "UT",
      "Zip": "84070",
      "List Price": 309990,
      "Status": "Active",
      "List Date": "2026-05-01",
      "MLS Number": "2138751",
      "Portal Slug": "8278-s-resaca-dr-l1-sandy-ut-84070-kvmx",
      "Portal URL": "https://portal.utahdigs.com/8278-s-resaca-dr-l1-sandy-ut-84070-kvmx",
      "Passcode": "5057",
      "Lofty Transaction ID": "1775045",
      "Sellers": [
        "recnyg3dtFgHxnxad"
      ],
      "Weekly Stats": [
        "rec0csrajaNvKYmcd",
        "rec3EUq6tIDyFp1Cr",
        "recSLJusuGrWE1nyw",
        "recReMunW0QEl0cre"
      ],
      "Showings": [
        "recsTNxFP1KzeWTln",
        "recQyAL1RnwsSqob9",
        "recbSPpH1egIWj5ML"
      ],
      "Beds": 3,
      "Baths": 2,
      "Sqft": 1244,
      "Blair Note": "A showing this week, and online traffic stays strong — nearly a thousand portal views in the last month. The listing is doing its job pulling people i...",
      "Blair Note Date": "2026-06-22",
      "Latest Views": 967,
      "Latest Saves": 9,
      "Latest Showings": 1,
      "Active Offers": 0,
      "Offers": [
        "recnYuAl33QJIUt4q",
        "rec6eru7ntdBRJE05",
        "recaBseMhjqjEa34v",
        "recZBCkuLKXNTesPv"
      ],
      "Seller Email": "blairjallen@gmail.com",
      "Seller Name": "Blair Allen",
      "Seller Phone": "(801) 337-5057",
      "Portal Email Sent": true,
      "Market Avg DOM": 45,
      "Escrow Officer": [
        "rec39Hafyn2Y9Jlla"
      ],
      "Transaction Coordinator": [
        "recgsIboMs68c7mg2"
      ]
    }
  },
  {
    "id": "reclBPWmG8sptzCbn",
    "fields": {
      "Address": "123 Test Lane, Salt Lake City, UT 84101",
      "Portal Slug": "123-test-lane-salt-lake-city-ut-84101-rjia",
      "Portal URL": "https://portal.utahdigs.com/123-test-lane-salt-lake-city-ut-84101-rjia",
      "Passcode": "5057",
      "Seller Email": "blairjallen@gmail.com",
      "Seller Name": "Blair Allen",
      "Seller Phone": "(801) 337-5057",
      "Portal Email Sent": true
    }
  },
  {
    "id": "rec5UloSEmdokLguV",
    "fields": {
      "Address": "330 E 7635 S, Sandy, UT 84047",
      "City": "Sandy",
      "State": "UT",
      "Zip": "84047",
      "List Price": 529700,
      "Status": "Active",
      "List Date": "2026-02-24",
      "MLS Number": "2139122",
      "Portal Slug": "330-e-7635-s-sandy-ut-84047-4vqo",
      "Portal URL": "https://portal.utahdigs.com/330-e-7635-s-sandy-ut-84047-4vqo",
      "Passcode": "0122",
      "Sellers": [
        "recsmpmLVS6BIRHWO",
        "recluGPutv9Lta0sd"
      ],
      "Weekly Stats": [
        "rec1r9hnaucau1RYA"
      ],
      "Showings": [
        "recUB7sPNXMRw4xVR",
        "recLRMvZ99fKrB1rH",
        "recRrAqtIbmh5y2dZ",
        "rect1iJ3tGCK6aJ46",
        "recz1Dx0OGFN6APWg",
        "recV8UGeNmmqjkZ6B",
        "recHxZ6RaaAcBGr1W",
        "rece8rK4NjWQCGdoK",
        "recZMCxxzIl5vZ1Eb",
        "recEXj0oMQD32wIEU",
        "recL4CLOQcZgSWwd0",
        "rec7MvhFt9uhSyj6F",
        "recohgjV7ROO8uCS5",
        "recopSaRHy1Zl6Z4e",
        "recEYms34yrMB33oc",
        "rec3LxmAICAHOQgEh",
        "recYi7hvl63jOdxCM",
        "recpb5e9R5RLB473T",
        "recejkzmMrYBEsKY4"
      ],
      "Blair Note": "One showing Saturday. Honest feedback — buyer liked the condition and thought the price was fair, but a townhouse with stairs wasn't their layout. Tha...",
      "Blair Note Date": "2026-06-22",
      "Latest Views": 284,
      "Latest Saves": 0,
      "Latest Showings": 1,
      "Active Offers": 0,
      "Seller Email": "blair.jallen@gmail.com",
      "Seller Name": "Brandon Fry",
      "Seller Phone": "801-718-1331",
      "Portal Email Sent": true,
      "Co-Seller 1 Email": "b.lairjallen@gmail.com",
      "Zillow URL": "https://www.zillow.com/homes/330-E-7635-S-Midvale--UT-84047/12880839_zpid/",
      "Portfolio Group": "brandon-fry",
      "Portfolio Passcode": "0122",
      "Market Avg DOM": 45,
      "Portfolio Token": "brandon-fry"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Open URL'));
  });
});
Output


Delete Listings records
To delete Listings records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Listings').destroy(['reckLQ0OlLD3B8sxl', 'reclBPWmG8sptzCbn'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Sellers Table
The id for Sellers is tblBoQZZbkGpIe5V7. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Sellers table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
NamefldfNalFBOja7gqGPText
string
A single line of text.
 
Example values
"Blair Allen (Test)"

"Brandon Fry"

"Justin Booth"

EmailfldkabVXdLqfdATiJEmail
string
A valid email address.
 
Example values
"blairjallen@gmail.com"

"blair.jallen@gmail.com"

"b.lairjallen@gmail.com"

PhonefldUZhhjXFZ3ubjCI
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"801-337-5057"

"801-718-1331"

Lofty Contact IDfldGO0Cfsot7JS8QbText
string
A single line of text.
 
Example values
"foo", "bar"

Listingsfld1uXBlc83jo709h
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

First Namefldm8eWwi8gT4TdNWText
string
A single line of text.
 
Example values
"Brandon"

"Justin"

Last NamefldKoHpNZ5LcRKvzKText
string
A single line of text.
 
Example values
"Fry"

"Booth"

RelationshipfldZkOiG8WPbDFG4h
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Spouse/Partner",
    "Co-Owner",
    "Family Member",
    "Other"
]
Property Address (Submitted)fldyZLg30Twx5r2P8Text
string
A single line of text.
 
Example values
"foo", "bar"

Additional Access Notesfld6PQ0w5SOspToOiLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example value
"Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque vel accumsan orci. Fusce interdum ipsum nec neque iaculis congue. Vivamus bibendum quam at sollicitudin vestibulum. Nunc suscipit nisl sit amet enim hendrerit pellentesque. Curabitur non fermentum orci."
Submitted DatefldxJRpJS4HqqenLu
Date and time
string (ISO 8601 formatted date)
UTC date and time, e.g. "2014-09-05T07:00:00.000Z".
 
Example value
"2014-09-05T07:00:00.000Z"

List Sellers records
To list records in Sellers, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Name and Email, pass in:

fields: ["Name", "Email"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldfNalFBOja7gqGP", "fldkabVXdLqfdATiJ"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Name isn't empty, pass in NOT({Name} = '') as a parameter like this:

filterByFormula: "NOT({Name} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Name in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Name
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Name in descending order, pass in:

[{field: "Name", direction: "desc"}]
view
string
optional
The name or ID of a view in the Sellers table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Sellers').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Name'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved Blair Allen (Test)
Retrieved Brandon Fry
Retrieved Justin Booth
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Sellers').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Name'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Sellers').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Sellers record
To retrieve an existing record in Sellers table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Sellers').find('recnyg3dtFgHxnxad', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved recnyg3dtFgHxnxad
Create Sellers records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

When creating date time values in Submitted Date, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Sellers').create([
  {
    "fields": {
      "Name": "Blair Allen (Test)",
      "Email": "blairjallen@gmail.com",
      "Phone": "801-337-5057",
      "Listings": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "fields": {
      "Name": "Brandon Fry",
      "Email": "blair.jallen@gmail.com",
      "Phone": "801-718-1331",
      "Listings": [
        "rec5UloSEmdokLguV",
        "recCEMxiKWq9wLL3r",
        "recETWHfHkESMmIb5",
        "reca6PPo584XVD0zI",
        "reciV3LnkrGMSRLNS",
        "rec82eRUounSwXXFn"
      ],
      "First Name": "Brandon",
      "Last Name": "Fry"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
recnyg3dtFgHxnxad
recsmpmLVS6BIRHWO
Update Sellers records
To update Sellers records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Sellers records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listings, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Additional Access Notes may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

When updating date time values in Submitted Date, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Sellers').update([
  {
    "id": "recnyg3dtFgHxnxad",
    "fields": {
      "Name": "Blair Allen (Test)",
      "Email": "blairjallen@gmail.com",
      "Phone": "801-337-5057",
      "Listings": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recsmpmLVS6BIRHWO",
    "fields": {
      "Name": "Brandon Fry",
      "Email": "blair.jallen@gmail.com",
      "Phone": "801-718-1331",
      "Listings": [
        "rec5UloSEmdokLguV",
        "recCEMxiKWq9wLL3r",
        "recETWHfHkESMmIb5",
        "reca6PPo584XVD0zI",
        "reciV3LnkrGMSRLNS",
        "rec82eRUounSwXXFn"
      ],
      "First Name": "Brandon",
      "Last Name": "Fry"
    }
  },
  {
    "id": "recluGPutv9Lta0sd",
    "fields": {
      "Name": "Justin Booth",
      "Email": "b.lairjallen@gmail.com",
      "Listings": [
        "rec5UloSEmdokLguV",
        "recCEMxiKWq9wLL3r",
        "recETWHfHkESMmIb5",
        "reca6PPo584XVD0zI",
        "reciV3LnkrGMSRLNS",
        "rec82eRUounSwXXFn"
      ],
      "First Name": "Justin",
      "Last Name": "Booth"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Listings'));
  });
});
Output
[
    "reckLQ0OlLD3B8sxl"
]
[
    "rec5UloSEmdokLguV",
    "recCEMxiKWq9wLL3r",
    "recETWHfHkESMmIb5",
    "reca6PPo584XVD0zI",
    "reciV3LnkrGMSRLNS",
    "rec82eRUounSwXXFn"
]
[
    "rec5UloSEmdokLguV",
    "recCEMxiKWq9wLL3r",
    "recETWHfHkESMmIb5",
    "reca6PPo584XVD0zI",
    "reciV3LnkrGMSRLNS",
    "rec82eRUounSwXXFn"
]
Delete Sellers records
To delete Sellers records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Sellers').destroy(['recnyg3dtFgHxnxad', 'recsmpmLVS6BIRHWO'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Weekly Stats Table
The id for Weekly Stats is tbl7xNeeU3QOj88ds. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Weekly Stats table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Week LabelfldvnM5r82x5aoE8hText
string
A single line of text.
 
Example values
"2138751 — Jun 19, 2026"

"2125501 — Jun 19, 2026"

"2152174 — Jun 19, 2026"

"2154039 — Jun 19, 2026"

"2154223 — Jun 19, 2026"

Week EndingfldSjkLUx57hH62sQ
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-19"

"2026-06-19"

"2026-06-19"

"2026-06-19"

"2026-06-19"

Zillow ViewsfldyX3KKlmerOSKU6Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

Realtor.com ViewsfldY9wVbwKXv6fMnTNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

Redfin ViewsfldiF3uZWUnogDSTmNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

Saves / Favoritesfldtlpzke1PFvZ5xMNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

SharesfldRB06niwmxw77RzNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
0

0

0

0

0

Source Email Datefld7qRwdWT4ZNPhie
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-21"

"2026-06-21"

Listingflde3xLwKgfRrHwvn
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Total ViewsfldnKBJcTl4kTcFKjFormula
number, string, array of numbers or strings
Computed value: {Zillow Views} + {Realtor.com Views} + {Redfin Views}.
 
Example values
0

0

0

0

0

utahrealestate.com ViewsfldGj3IbskyQ7t4d5Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
482

Inquiriesfldx8V1C253Z37cOQNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
1

Lifetime ViewsfldrduoP3ZdXmKwJ8Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
4311

Zillow New Saves This WeekfldOVK6ePzXQuSvshNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

URE Views (30d)fldzMdKbT91kSZLrWNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
482

482

290

255

208

Zillow Views (30d)fldu194IWw2kI1NMaNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
369

369

324

276

114

Realtor.com Views (30d)fld8COkHDGCBB234jNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
154

154

48

30

9

Homes.com Views (30d)fld6cJOeNNEdJLjN9Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
28

28

30

5

11

Trulia Views (30d)fldANWghAEanYzv5iNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
12

12

15

16

2

Listtrac Total (30d)fldzoCByJtkYEgmbYNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
1062

1062

721

591

346

URE Favorites (cumulative)fldKRQg9U6PR6ZHe8Number
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
10

1

1

3

2

List Weekly Stats records
To list records in Weekly Stats, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Week Label and Week Ending, pass in:

fields: ["Week Label", "Week Ending"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldvnM5r82x5aoE8h", "fldSjkLUx57hH62sQ"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Week Label isn't empty, pass in NOT({Week Label} = '') as a parameter like this:

filterByFormula: "NOT({Week Label} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Week Label in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Week%20Label
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Week Label in descending order, pass in:

[{field: "Week Label", direction: "desc"}]
view
string
optional
The name or ID of a view in the Weekly Stats table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Weekly Stats').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Week Label'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved 2138751 — Jun 19, 2026
Retrieved 2125501 — Jun 19, 2026
Retrieved 2152174 — Jun 19, 2026
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Weekly Stats').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Week Label'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Weekly Stats').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Weekly Stats record
To retrieve an existing record in Weekly Stats table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Weekly Stats').find('rec0csrajaNvKYmcd', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved rec0csrajaNvKYmcd
Create Weekly Stats records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

Values for Total Views are automatically computed by Airtable and cannot be directly created.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Weekly Stats').create([
  {
    "fields": {
      "Week Label": "2138751 — Jun 19, 2026",
      "Week Ending": "2026-06-19",
      "Zillow Views": 0,
      "Realtor.com Views": 0,
      "Redfin Views": 0,
      "Saves / Favorites": 0,
      "Shares": 0,
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "fields": {
      "Week Label": "2125501 — Jun 19, 2026",
      "Week Ending": "2026-06-19",
      "Zillow Views": 0,
      "Realtor.com Views": 0,
      "Redfin Views": 0,
      "Saves / Favorites": 0,
      "Shares": 0
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
rec0csrajaNvKYmcd
recp7FgAV2GdhHxrG
Update Weekly Stats records
To update Weekly Stats records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Weekly Stats records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Values for Total Views are automatically computed by Airtable and cannot be directly updated. You cannot clear these, even with a replace call.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Weekly Stats').update([
  {
    "id": "rec0csrajaNvKYmcd",
    "fields": {
      "Week Label": "2138751 — Jun 19, 2026",
      "Week Ending": "2026-06-19",
      "Zillow Views": 0,
      "Realtor.com Views": 0,
      "Redfin Views": 0,
      "Saves / Favorites": 0,
      "Shares": 0,
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recp7FgAV2GdhHxrG",
    "fields": {
      "Week Label": "2125501 — Jun 19, 2026",
      "Week Ending": "2026-06-19",
      "Zillow Views": 0,
      "Realtor.com Views": 0,
      "Redfin Views": 0,
      "Saves / Favorites": 0,
      "Shares": 0
    }
  },
  {
    "id": "rec5IXNsmvpzlNblf",
    "fields": {
      "Week Label": "2152174 — Jun 19, 2026",
      "Week Ending": "2026-06-19",
      "Zillow Views": 0,
      "Realtor.com Views": 0,
      "Redfin Views": 0,
      "Saves / Favorites": 0,
      "Shares": 0
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Shares'));
  });
});
Output
0
0
0
Delete Weekly Stats records
To delete Weekly Stats records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Weekly Stats').destroy(['rec0csrajaNvKYmcd', 'recp7FgAV2GdhHxrG'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Showings Table
The id for Showings is tblgQQizi4zE22L7q. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Showings table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Showing LabelfldsHOgqyS2MPOKvfText
string
A single line of text.
 
Example values
"Jun 12 @ 11:00 AM"

"Jun 14 @ 2:00 PM"

"Mar 10 @ 4:00 PM"

"Mar 11 @ 1:20 PM"

"Mar 11 @ 4:45 PM"

Showing DatefldQ1LUFl1h9uwSgc
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-12"

"2026-06-14"

"2026-03-10"

"2026-03-11"

"2026-03-11"

Showing TimefldpphayrpBdPAL7MText
string
A single line of text.
 
Example values
"11:00 AM"

"2:00 PM"

"4:00 PM"

"1:20 PM"

"4:45 PM"

Buyer's AgentfldYTEanShd5Za8jIText
string
A single line of text.
 
Example values
"Sarah Johnson"

"Ryan Davis"

"Paul Wiseman / Summit Sotheby's International Realty"

"Thomas Moffat / Equity Real Estate (Solid)"

"Paul Wiseman / Summit Sotheby's International Realty"

Feedbackfld8U9QhaZODTnoHWLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example values
"Great layout, loved the kitchen. My clients are very serious about this price range — they've been looking for three months and this hits everything o..."

"Buyers really loved the space and the natural light. They're comparing with one other property but said this one is the frontrunner. Expect to hear ba..."

"Condition: Excellent / Interest: Maybe / Offer: Maybe"

"Condition: Good / Price: A bit high / Interest: No / Offer: No / Comment: Wanting something with more outdoor space and access."

"Condition: Good / Price: Fair / Interest: Maybe / Offer: Maybe"

Feedback Scorefld96g9ZWUQgNDeK6
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Positive",
    "Neutral",
    "Negative",
    "No Feedback"
]
Source Email DatefldiSIntnLwhSXJhs
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example value
"2014-09-05"

Listingfld58UFA70mmnlFjj
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

List Showings records
To list records in Showings, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Showing Label and Showing Date, pass in:

fields: ["Showing Label", "Showing Date"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldsHOgqyS2MPOKvf", "fldQ1LUFl1h9uwSgc"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Showing Label isn't empty, pass in NOT({Showing Label} = '') as a parameter like this:

filterByFormula: "NOT({Showing Label} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Showing Label in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Showing%20Label
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Showing Label in descending order, pass in:

[{field: "Showing Label", direction: "desc"}]
view
string
optional
The name or ID of a view in the Showings table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Showings').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Showing Label'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved Jun 12 @ 11:00 AM
Retrieved Jun 14 @ 2:00 PM
Retrieved Mar 10 @ 4:00 PM
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Showings').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Showing Label'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Showings').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Showings record
To retrieve an existing record in Showings table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Showings').find('recsTNxFP1KzeWTln', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved recsTNxFP1KzeWTln
Create Showings records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Showings').create([
  {
    "fields": {
      "Showing Label": "Jun 12 @ 11:00 AM",
      "Showing Date": "2026-06-12",
      "Showing Time": "11:00 AM",
      "Buyer's Agent": "Sarah Johnson",
      "Feedback": "Great layout, loved the kitchen. My clients are very serious about this price range — they've been looking for three months and this hits everything o...",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "fields": {
      "Showing Label": "Jun 14 @ 2:00 PM",
      "Showing Date": "2026-06-14",
      "Showing Time": "2:00 PM",
      "Buyer's Agent": "Ryan Davis",
      "Feedback": "Buyers really loved the space and the natural light. They're comparing with one other property but said this one is the frontrunner. Expect to hear ba...",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
recsTNxFP1KzeWTln
recQyAL1RnwsSqob9
Update Showings records
To update Showings records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Showings records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Feedback may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Showings').update([
  {
    "id": "recsTNxFP1KzeWTln",
    "fields": {
      "Showing Label": "Jun 12 @ 11:00 AM",
      "Showing Date": "2026-06-12",
      "Showing Time": "11:00 AM",
      "Buyer's Agent": "Sarah Johnson",
      "Feedback": "Great layout, loved the kitchen. My clients are very serious about this price range — they've been looking for three months and this hits everything o...",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recQyAL1RnwsSqob9",
    "fields": {
      "Showing Label": "Jun 14 @ 2:00 PM",
      "Showing Date": "2026-06-14",
      "Showing Time": "2:00 PM",
      "Buyer's Agent": "Ryan Davis",
      "Feedback": "Buyers really loved the space and the natural light. They're comparing with one other property but said this one is the frontrunner. Expect to hear ba...",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recUB7sPNXMRw4xVR",
    "fields": {
      "Showing Label": "Mar 10 @ 4:00 PM",
      "Showing Date": "2026-03-10",
      "Showing Time": "4:00 PM",
      "Buyer's Agent": "Paul Wiseman / Summit Sotheby's International Realty",
      "Listing": [
        "rec5UloSEmdokLguV"
      ]
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Listing'));
  });
});
Output
[
    "reckLQ0OlLD3B8sxl"
]
[
    "reckLQ0OlLD3B8sxl"
]
[
    "rec5UloSEmdokLguV"
]
Delete Showings records
To delete Showings records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Showings').destroy(['recsTNxFP1KzeWTln', 'recQyAL1RnwsSqob9'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Market Data Table
The id for Market Data is tblY3CYT2e6mkOjey. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Market Data table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Market Week LabelfldSnA1fxxRXKePV3Text
string
A single line of text.
 
Example values
"Sandy — Jun 22"

Week Endingfld4aB6B0UD0g9Zb3
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-22"

Avg DOMfld2vGTRGqP5WiCbDNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
45

Median List PricefldbT4OqohjHuEQg9Currency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

List-to-Sale RatiofldBzWwURgd8hIl7MPercent
number
Decimal number representing a percentage value. For example, the underlying cell value for 12.3% is 0.123. This field only allows positive numbers.
 
Example value

Price Reductions PctfldeVBZ8tM6nBHKZxPercent
number
Decimal number representing a percentage value. For example, the underlying cell value for 12.3% is 0.123. This field only allows positive numbers.
 
Example value

New ListingsfldPO2f4OMLrlcpoeNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Sold CountfldrNy1HmgM00q50oNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Listingfld5EZhn7F1GDKkhP
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Cityfldtx7dFprwIwY4CmText
string
A single line of text.
 
Example values
"Sandy"

Report DatefldBrmw8iAsH9VYqH
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-22"

Homes For SalefldwSdD5H9nB4JEhfNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
485

Homes For Sale Change PctfldSYa3GdVWyR5Cy5Number
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
10.6

New To MarketfldnDPFOpjkZasbbfNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
103

New To Market Change PctfldZqpMJymVobRsHQNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
-12.9

DOM Change PctfldEci3wrN1JYlH42Number
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
11.6

Avg Home PricefldSK56mHnYESqryUCurrency
number
Currency value. This field only allows positive numbers.
 
Example values
1004981

Avg Home Price Change PctfldxP9lUf0Fp33CtTNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
1.5

Price Reductions CountfldBC6MAJ3OZmiQ8bNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
109

Price Reductions Change PctfldcqmZWt7Se8CNrqNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
75.8

Price Per Sq FtfldDbe2uNexehyOHZCurrency
number
Currency value. This field only allows positive numbers.
 
Example values
308

Price Per Sq Ft Change Pctfld5ZGyaaBDnZaWGaNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
1.0

Avg Sold PricefldkE6ZtfQzyUHhr0Currency
number
Currency value. This field only allows positive numbers.
 
Example values
909772

Avg Sold Price Change PctfldggAOnbWKA0Pa3uNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
28.6

Homes Sold CountfldoxpICnpqeitN6NNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example values
61

Homes Sold Change PctfldW3kw1PeDNExpouNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
-34.4

Sold To Listed RatiofldalyPjUaztt0NhQText
string
A single line of text.
 
Example values
"61:103"

Sold To Listed Change PctfldJQmVOUF5dnlHADNumber
number
A decimal number showing 1 decimal digit. This field only allows positive numbers.
 
Example values
-29.9

List Market Data records
To list records in Market Data, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Market Week Label and Week Ending, pass in:

fields: ["Market Week Label", "Week Ending"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldSnA1fxxRXKePV3", "fld4aB6B0UD0g9Zb3"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Market Week Label isn't empty, pass in NOT({Market Week Label} = '') as a parameter like this:

filterByFormula: "NOT({Market Week Label} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Market Week Label in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Market%20Week%20Label
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Market Week Label in descending order, pass in:

[{field: "Market Week Label", direction: "desc"}]
view
string
optional
The name or ID of a view in the Market Data table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Market Data').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Market Week Label'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved Sandy — Jun 22
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Market Data').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Market Week Label'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Market Data').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Market Data record
To retrieve an existing record in Market Data table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Market Data').find('rec8XmItFdqx49JgP', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved rec8XmItFdqx49JgP
Create Market Data records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Market Data').create([
  {
    "fields": {
      "Market Week Label": "Sandy — Jun 22",
      "Week Ending": "2026-06-22",
      "Avg DOM": 45,
      "City": "Sandy",
      "Report Date": "2026-06-22",
      "Homes For Sale": 485,
      "Homes For Sale Change Pct": 10.6,
      "New To Market": 103,
      "New To Market Change Pct": -12.9,
      "DOM Change Pct": 11.6,
      "Avg Home Price": 1004981,
      "Avg Home Price Change Pct": 1.5,
      "Price Reductions Count": 109,
      "Price Reductions Change Pct": 75.8,
      "Price Per Sq Ft": 308,
      "Price Per Sq Ft Change Pct": 1,
      "Avg Sold Price": 909772,
      "Avg Sold Price Change Pct": 28.6,
      "Homes Sold Count": 61,
      "Homes Sold Change Pct": -34.4,
      "Sold To Listed Ratio": "61:103",
      "Sold To Listed Change Pct": -29.9
    }
  },
  {
    "fields": {
      "Market Week Label": "Sandy — Jun 22",
      "Week Ending": "2026-06-22",
      "Avg DOM": 45,
      "City": "Sandy",
      "Report Date": "2026-06-22",
      "Homes For Sale": 485,
      "Homes For Sale Change Pct": 10.6,
      "New To Market": 103,
      "New To Market Change Pct": -12.9,
      "DOM Change Pct": 11.6,
      "Avg Home Price": 1004981,
      "Avg Home Price Change Pct": 1.5,
      "Price Reductions Count": 109,
      "Price Reductions Change Pct": 75.8,
      "Price Per Sq Ft": 308,
      "Price Per Sq Ft Change Pct": 1,
      "Avg Sold Price": 909772,
      "Avg Sold Price Change Pct": 28.6,
      "Homes Sold Count": 61,
      "Homes Sold Change Pct": -34.4,
      "Sold To Listed Ratio": "61:103",
      "Sold To Listed Change Pct": -29.9
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
rec8XmItFdqx49JgP
rec8XmItFdqx49JgP
Update Market Data records
To update Market Data records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Market Data records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Market Data').update([
  {
    "id": "rec8XmItFdqx49JgP",
    "fields": {
      "Market Week Label": "Sandy — Jun 22",
      "Week Ending": "2026-06-22",
      "Avg DOM": 45,
      "City": "Sandy",
      "Report Date": "2026-06-22",
      "Homes For Sale": 485,
      "Homes For Sale Change Pct": 10.6,
      "New To Market": 103,
      "New To Market Change Pct": -12.9,
      "DOM Change Pct": 11.6,
      "Avg Home Price": 1004981,
      "Avg Home Price Change Pct": 1.5,
      "Price Reductions Count": 109,
      "Price Reductions Change Pct": 75.8,
      "Price Per Sq Ft": 308,
      "Price Per Sq Ft Change Pct": 1,
      "Avg Sold Price": 909772,
      "Avg Sold Price Change Pct": 28.6,
      "Homes Sold Count": 61,
      "Homes Sold Change Pct": -34.4,
      "Sold To Listed Ratio": "61:103",
      "Sold To Listed Change Pct": -29.9
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Avg DOM'));
  });
});
Output
45
Delete Market Data records
To delete Market Data records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Market Data').destroy(['rec8XmItFdqx49JgP'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 1 records
Seller Requests Table
The id for Seller Requests is tblLccmOrodONrfJs. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Seller Requests table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Request SummaryfldotuQ5MqSzXy067Text
string
A single line of text.
 
Example values
"TEST — Automation check"

"Message from Blair — 8278 S RESACA DR #L1, Sandy, UT 84070"

"Description update — 8278 S RESACA DR #L1, Sandy, UT 84070"

"Open House request — 8278 S RESACA DR #L1, Sandy, UT 84070"

"Message from Blair — 8278 S RESACA DR #L1, Sandy, UT 84070"

Request TypefldzUxoo7fCIdqSVR
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Price Reduction",
    "Listing Update",
    "Question",
    "Doc Upload"
]
MessagefldSyUXAVWWq3fqMHLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example values
"This is a test submission to verify both Resend automations are firing correctly. Please ignore."

"How about this one?"

"Dude.  Does this work?"

"Hey there.  This is to test 2 way communication?"

"Yo"

SubmittedfldjaA8Kkz3syW1un
Date and time
string (ISO 8601 formatted date)
UTC date and time, e.g. "2014-09-05T07:00:00.000Z".
 
Example values
"2026-06-25T05:21:48.493Z"

"2026-06-25T05:55:36.326Z"

"2026-06-25T05:55:44.544Z"

"2026-06-25T18:20:29.286Z"

"2026-06-26T05:15:56.112Z"

StatusfldADvI6CZNzQcoRr
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Open",
    "Reviewed",
    "Resolved"
]
Blair's ResponsefldaBQpTkek8ZymItLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example values
"Yes it worked. How does it look on your end?\n"

ListingfldCQIsaXhxUzuTDz
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Seller Namefld3Jafob6JRqMq2mText
string
A single line of text.
 
Example values
"Blair Allen (TEST)"

"Blair Allen (Test)"

"Blair Allen (Test)"

"Blair Allen (Test)"

"Blair Allen (Test)"

Seller EmailfldApia0UafS0Xz6PEmail
string
A valid email address.
 
Example values
"blairjallen@gmail.com"

"blairjallen@gmail.com"

"blairjallen@gmail.com"

"blairjallen@gmail.com"

"blairjallen@gmail.com"

Property Addressfld6YNvd1vo3Iu9P2Text
string
A single line of text.
 
Example values
"123 Test Lane, Salt Lake City, UT 84101"

"8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"

"8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"

"8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"

"8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"

Update TypesfldU3KHIwTIiy6nsg
Multiple select
array of strings
Array of selected option names.

When creating or updating records, if a choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Price Change",
    "Photos",
    "Description / Remarks",
    "Open House"
]
Current PricefldZbDdqNRsB3tdnVCurrency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

New PricefldO76cBbLACq8FCmCurrency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

Open House Date/Timefld0VB1KjCQL9Rp85
Date and time
string (ISO 8601 formatted date)
UTC date and time, e.g. "2014-09-05T07:00:00.000Z".
 
Example values
"2026-06-25T17:00:00.000Z"

Photosfldbsy6R09FYZUbXX
Attachment
array of attachment objects
Each attachment object may contain the following properties. To see which fields are required or optional, please consult the relevant section: retrieve, create, update, or delete.

idstring
unique attachment id
urlstring
url, e.g. "https://v5.airtableusercontent.com/foo".

Note: URLs returned will expire 2 hours after being returned from our API. If you want to persist the attachments, we recommend downloading them instead of saving the URL. See our support article for more information.
filenamestring
filename, e.g. "foo.jpg"
sizenumber
file size, in bytes
typestring
content type, e.g. "image/jpeg"
widthnumberheightnumber
width/height, in pixels (these may be available if the attachment is an image)
thumbnails.small.urlstringthumbnails.large.urlstring
url of small/large thumbnails (these may be available if the attachment is an image or document). See notes under url about the lifetime of these URLs.
thumbnails.small.widthnumberthumbnails.small.heightnumberthumbnails.large.widthnumberthumbnails.large.heightnumber
width/height of small/large thumbnails, in pixels (these will be available if the corresponding thumbnail url is available)
 
Example value
[
    {
        "id": "att6f75cc83f1b648",
        "size": 26317,
        "url": "https://www.filepicker.io/api/file/5YTJXioCQG0tYWPw6OPw",
        "type": "image/jpeg",
        "filename": "33823_3_xl.jpg",
        "thumbnails": {
            "small": {
                 "url": "https://www.filepicker.io/api/file/Dy5gioxaShSUvHX0LgIC",
                 "width": 54,
                 "height": 36
             },
            "large": {
                 "url": "https://www.filepicker.io/api/file/ueYi00yRiqhuUn420UZA",
                 "width": 197,
                 "height": 131
             }
         }
    }
]
                            

DocumentsfldXSTxzjBpkiwjF6
Attachment
array of attachment objects
Each attachment object may contain the following properties. To see which fields are required or optional, please consult the relevant section: retrieve, create, update, or delete.

idstring
unique attachment id
urlstring
url, e.g. "https://v5.airtableusercontent.com/foo".

Note: URLs returned will expire 2 hours after being returned from our API. If you want to persist the attachments, we recommend downloading them instead of saving the URL. See our support article for more information.
filenamestring
filename, e.g. "foo.jpg"
sizenumber
file size, in bytes
typestring
content type, e.g. "image/jpeg"
widthnumberheightnumber
width/height, in pixels (these may be available if the attachment is an image)
thumbnails.small.urlstringthumbnails.large.urlstring
url of small/large thumbnails (these may be available if the attachment is an image or document). See notes under url about the lifetime of these URLs.
thumbnails.small.widthnumberthumbnails.small.heightnumberthumbnails.large.widthnumberthumbnails.large.heightnumber
width/height of small/large thumbnails, in pixels (these will be available if the corresponding thumbnail url is available)
 
Example value
[
    {
        "id": "att6f75cc83f1b648",
        "size": 26317,
        "url": "https://www.filepicker.io/api/file/5YTJXioCQG0tYWPw6OPw",
        "type": "image/jpeg",
        "filename": "33823_3_xl.jpg",
        "thumbnails": {
            "small": {
                 "url": "https://www.filepicker.io/api/file/Dy5gioxaShSUvHX0LgIC",
                 "width": 54,
                 "height": 36
             },
            "large": {
                 "url": "https://www.filepicker.io/api/file/ueYi00yRiqhuUn420UZA",
                 "width": 197,
                 "height": 131
             }
         }
    }
]
                            

List Seller Requests records
To list records in Seller Requests, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Request Summary and Request Type, pass in:

fields: ["Request Summary", "Request Type"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldotuQ5MqSzXy067", "fldzUxoo7fCIdqSVR"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Request Summary isn't empty, pass in NOT({Request Summary} = '') as a parameter like this:

filterByFormula: "NOT({Request Summary} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Request Summary in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Request%20Summary
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Request Summary in descending order, pass in:

[{field: "Request Summary", direction: "desc"}]
view
string
optional
The name or ID of a view in the Seller Requests table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Seller Requests').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Request Summary'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved TEST — Automation check
Retrieved Message from Blair — 8278 S RESACA DR #L1, Sandy, UT 84070
Retrieved Description update — 8278 S RESACA DR #L1, Sandy, UT 84070
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Seller Requests').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Request Summary'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Seller Requests').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Seller Requests record
To retrieve an existing record in Seller Requests table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

In attachment objects included in the retrieved record (Photos and Documents), only id, url, and filename are always returned. Other attachment properties may not be included. Note: Attachment URLs returned will expire 2 hours after being returned from our API. If you want to persist the attachments, we recommend downloading them instead of saving the URL. See our support article for more information.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Seller Requests').find('recgQuSwAkZOCKM3C', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved recgQuSwAkZOCKM3C
Create Seller Requests records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

To create new attachments in Photos and Documents, set the field value to an array of attachment objects. When creating an attachment, url is required, and filename is optional. Airtable will download the file at the given url and keep its own copy of it. All other attachment object properties will be generated server-side soon afterward.

Note that in most cases the API does not currently return an error code for failed attachment object creation given attachment uploading happens in an asynchronous manner, such cases will manifest with the attachment object either being cleared from the cell or persisted with generated URLs that return error responses when queried. If the same attachment URL fails to upload multiple times in a short time interval then * the API may return an ATTACHMENTS_FAILED_UPLOADING error code in the details field of the response and the attachment object will * be cleared from the cell synchronously.

We also require URLs used to upload have the https:// or http:// protocol (Note: http:// support will be removed in the near future), have a limit of 3 max redirects, and a file size limit of 1GB. In addition, URLs must be publicly accessible, in cases where cookie authentication or logging in to access the file is required, the login page HTML will be downloaded instead of the file.

Attachment URLs returned will expire 2 hours after being returned from our API. If you want to persist the attachments, we recommend downloading them instead of saving the URL.

If too many attachments are uploaded within a short period of time, the server may return a partial failure on record creation with an "Attachment Upload Rate Too High" error. See our support article for more information.

When creating date time values in Submitted and Open House Date/Time, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Seller Requests').create([
  {
    "fields": {
      "Request Summary": "TEST — Automation check",
      "Message": "This is a test submission to verify both Resend automations are firing correctly. Please ignore.",
      "Seller Name": "Blair Allen (TEST)",
      "Seller Email": "blairjallen@gmail.com",
      "Property Address": "123 Test Lane, Salt Lake City, UT 84101"
    }
  },
  {
    "fields": {
      "Request Summary": "Message from Blair — 8278 S RESACA DR #L1, Sandy, UT 84070",
      "Message": "How about this one?",
      "Submitted": "2026-06-25T05:21:48.493Z",
      "Status": "Open",
      "Seller Name": "Blair Allen (Test)",
      "Seller Email": "blairjallen@gmail.com",
      "Property Address": "8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
recgQuSwAkZOCKM3C
recDKlT7xyXshwjS8
Update Seller Requests records
To update Seller Requests records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Seller Requests records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To add attachments to Photos and Documents, add new attachment objects to the existing array. Be sure to include all existing attachment objects that you wish to retain, to keep preexisting attachments providing id is required (which can be retrieved using the retrieve endpoint), other fields are ignored. For the new attachments being added, url is required, and filename is optional. To remove attachments, include the existing array of attachment objects, excluding any that you wish to remove.

Note that in most cases the API does not currently return an error code for failed attachment object creation given attachment uploading happens in an asynchronous manner, such cases will manifest with the attachment object either being cleared from the cell or persisted with generated URLs that return error responses when queried. If the same attachment URL fails to upload multiple times in a short time interval then * the API may return an ATTACHMENTS_FAILED_UPLOADING error code in the details field of the response and the attachment object will * be cleared from the cell synchronously.

We also require URLs used to upload have the https:// or http:// protocol (Note: http:// support will be removed in the near future), have a limit of 3 max redirects, and a file size limit of 1GB. In addition, URLs must be publicly accessible, in cases where cookie authentication or logging in to access the file is required, the login page HTML will be downloaded instead of the file.

If too many attachments are uploaded within a short period of time, the server may return a partial failure on record creation with an "Attachment Upload Rate Too High" error.

To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

To select new options in Update Types, add new option names to the existing array. Be sure to include all existing option names that you wish to retain. To deselect options, include the existing array of option names, excluding any that you wish to deselect.

Message and Blair's Response may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

When updating date time values in Submitted and Open House Date/Time, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Seller Requests').update([
  {
    "id": "recgQuSwAkZOCKM3C",
    "fields": {
      "Request Summary": "TEST — Automation check",
      "Message": "This is a test submission to verify both Resend automations are firing correctly. Please ignore.",
      "Seller Name": "Blair Allen (TEST)",
      "Seller Email": "blairjallen@gmail.com",
      "Property Address": "123 Test Lane, Salt Lake City, UT 84101"
    }
  },
  {
    "id": "recDKlT7xyXshwjS8",
    "fields": {
      "Request Summary": "Message from Blair — 8278 S RESACA DR #L1, Sandy, UT 84070",
      "Message": "How about this one?",
      "Submitted": "2026-06-25T05:21:48.493Z",
      "Status": "Open",
      "Seller Name": "Blair Allen (Test)",
      "Seller Email": "blairjallen@gmail.com",
      "Property Address": "8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"
    }
  },
  {
    "id": "recVajwWHb2CKDANR",
    "fields": {
      "Request Summary": "Description update — 8278 S RESACA DR #L1, Sandy, UT 84070",
      "Message": "Dude.  Does this work?",
      "Submitted": "2026-06-25T05:55:36.326Z",
      "Status": "Open",
      "Seller Name": "Blair Allen (Test)",
      "Seller Email": "blairjallen@gmail.com",
      "Property Address": "8278 S RESACA DR #L1, Sandy, UT 84070, Sandy, UT 84070"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Seller Name'));
  });
});
Output
"Blair Allen (TEST)"
"Blair Allen (Test)"
"Blair Allen (Test)"
Delete Seller Requests records
To delete Seller Requests records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Seller Requests').destroy(['recgQuSwAkZOCKM3C', 'recDKlT7xyXshwjS8'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Status Queue Table
The id for Status Queue is tblKFLbPhdHDfa2lj. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Status Queue table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
MLS Numberfld0dlx6e09H9CGY0Text
string
A single line of text.
 
Example values
"foo", "bar"

New StatusfldPGZxOhBjokzzxnText
string
A single line of text.
 
Example values
"foo", "bar"

List Status Queue records
To list records in Status Queue, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from MLS Number and New Status, pass in:

fields: ["MLS Number", "New Status"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fld0dlx6e09H9CGY0", "fldPGZxOhBjokzzxn"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where MLS Number isn't empty, pass in NOT({MLS Number} = '') as a parameter like this:

filterByFormula: "NOT({MLS Number} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by MLS Number in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=MLS%20Number
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by MLS Number in descending order, pass in:

[{field: "MLS Number", direction: "desc"}]
view
string
optional
The name or ID of a view in the Status Queue table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Status Queue').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('MLS Number'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
To see an example, please add some non-empty records to Status Queue.
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Status Queue').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('MLS Number'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Status Queue').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Status Queue record
To retrieve an existing record in Status Queue table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Example request & response
To see an example, please add some non-empty records to Status Queue.
Create Status Queue records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in. Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Status Queue.
Update Status Queue records
To update Status Queue records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Status Queue records will always fail.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Status Queue.
Delete Status Queue records
To delete Status Queue records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. 
Example request & response
To see an example, please add some non-empty records to Status Queue.
Offers Table
The id for Offers is tblrkfPpQYHWoURZf. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Offers table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Offer NamefldMxhaRj5CAGSjAAText
string
A single line of text.
 
Example values
"TEST — Offer #2 (Chen)"

Accept OfferfldbT2FC8NFclEZiZ
Button
button object
Object providing details about the button configuration.

labelstring
button label
urlstring
for "Open URL" actions, the computed url value
 
Example value
{
    "label": "✓ Accept",
    "url": "https://airtable.com"
}

Offer PricefldV5OcPA5aneIOgmCurrency
number
Currency value. This field only allows positive numbers.
 
Example values
358000

315000

305000

308000

Buyer's AgentfldzfCXNJUP4aupQBText
string
A single line of text.
 
Example values
"Mike Chen"

"Fred Flintstone"

"Billy Dean"

"James Dean"

Submitted DatefldgVZS9M7jKVpQ3k
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-06-14"

"2026-06-24"

"2026-06-24"

"2026-06-24"

StatusfldE7cs3TS8ZnTtqY
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Pending Review",
    "Accepted",
    "Declined",
    "Expired",
    "Cancelled"
]
Contract Pricefldd51mggpCAXSxTtCurrency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

Seller ConcessionsfldRqtlg7d8e5EN44Currency
number
Currency value. This field only allows positive numbers.
 
Example values
10000

5000

6800

Home Warranty Costfld2U0FgK4WsQhsAWCurrency
number
Currency value. This field only allows positive numbers.
 
Example value
42.21

Seller Disclosure DeadlinefldsafuURpruzvjxW
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example value
"2014-09-05"

Buyer Due Diligence DeadlinefldQ2S6BWXcPPege8
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-07-08"

"2026-07-10"

"2026-07-08"

Financing/Appraisal Deadlinefld5RUGZFBFWbMGjb
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example value
"2014-09-05"

Settlement DatefldBKrnw91A68H0z8
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example values
"2026-07-24"

"2026-07-24"

"2026-07-27"

NotesfldadZBc9KBm8poaLLong text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example values
"TEST RECORD — delete after review. FHA offer, below ask, countered — buyer walked."

"THIS IS A TEST!!!   "

"Blah blah"

"TESTRSRTSGDSGGSDF"

Listingfld2OXINakiVyeajf
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Agent Emailfldvylehu6y71liuVEmail
string
A valid email address.
 
Example values
"mchen@remax.com"

"fredflintstonebr@outlook.com"

"bjallen13@hotmail.com"

"blairallen@outlook.com"

Agent PhonefldrFCtP9ZtDAVksW
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"801-555-0447"

"(801) 801-8001"

"(801) 801-8001"

"(801) 485-5200"

Agent BrokeragefldpmQisgsILAZ3ysText
string
A single line of text.
 
Example values
"RE/MAX"

Buyer Name(s)fldnOtBUKziE6SoM3Text
string
A single line of text.
 
Example values
"James & Karen Chen"

"Barney Rubble"

"Billy Mac"

"Mr Bean"

Purchase TypefldZsHTyivi9mAUu5
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Owner Occupied",
    "Investor",
    "Other"
]
Financing TypefldEqoO1mDcx6JdYw
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Cash",
    "Conventional",
    "FHA",
    "VA",
    "Jumbo",
    "Hard Money",
    "USDA",
    "203K",
    "Other"
]
Earnest Money Depositfld9IQ3uc92XVGSghCurrency
number
Currency value. This field only allows positive numbers.
 
Example values
3500.00

10000.00

3000.00

4000.00

Proof of FundsfldRnhMgJZttFT3n8
Checkbox
boolean
This field is "true" when checked and otherwise empty.
 
Example value
true

Offer PDF URLfldTYvfrR1pRBKTPkURL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://www.jotform.com/inbox/6580919375241673545"

"https://www.jotform.com/inbox/6580940325245719919"

"https://www.jotform.com/inbox/6580954835244893022"

Listing AddressfldiHCbaluIjY93J1Text
string
A single line of text.
 
Example values
"8278 S RESACA DR #L1, Sandy, UT 84070"

"8278 S RESACA DR #L1, Sandy, UT 84070"

"8278 S RESACA DR #L1, Sandy, UT 84070"

Executed Offer URLfldINX2ZOeOOufRyHURL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://airtable.com", "http://minesweeperonline.com"

Home WarrantyfldDo3xOJNnahW9oI
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Yes",
    "No"
]
Street AddressfldDvVq2nBVxmIsAQFormula
number, string, array of numbers or strings
Computed value: TRIM(LEFT({Listing Address}, IF(FIND(',', {Listing Address}) > 0, FIND(',', {Listing Address}) - 1, LEN({Listing Address})))).
 
Example values
"\"[object Object]\""

"\"8278 S RESACA DR #L1\""

"\"8278 S RESACA DR #L1\""

"\"8278 S RESACA DR #L1\""

List Offers records
To list records in Offers, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Offer Name and Accept Offer, pass in:

fields: ["Offer Name", "Accept Offer"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldMxhaRj5CAGSjAA", "fldbT2FC8NFclEZiZ"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Offer Name isn't empty, pass in NOT({Offer Name} = '') as a parameter like this:

filterByFormula: "NOT({Offer Name} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Offer Name in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Offer%20Name
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Offer Name in descending order, pass in:

[{field: "Offer Name", direction: "desc"}]
view
string
optional
The name or ID of a view in the Offers table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Offers').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Offer Name'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved TEST — Offer #2 (Chen)
Retrieved 
Retrieved 
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Offers').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Offer Name'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Offers').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Offers record
To retrieve an existing record in Offers table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Offers').find('recnYuAl33QJIUt4q', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved recnYuAl33QJIUt4q
Create Offers records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

Values for Accept Offer and Street Address are automatically computed by Airtable and cannot be directly created.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Offers').create([
  {
    "fields": {
      "Offer Name": "TEST — Offer #2 (Chen)",
      "Offer Price": 358000,
      "Buyer's Agent": "Mike Chen",
      "Submitted Date": "2026-06-14",
      "Status": "Declined",
      "Notes": "TEST RECORD — delete after review. FHA offer, below ask, countered — buyer walked.",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ],
      "Agent Email": "mchen@remax.com",
      "Agent Phone": "801-555-0447",
      "Agent Brokerage": "RE/MAX",
      "Buyer Name(s)": "James & Karen Chen",
      "Purchase Type": "Owner Occupied",
      "Financing Type": "FHA",
      "Earnest Money Deposit": 3500,
      "Proof of Funds": true
    }
  },
  {
    "fields": {
      "Offer Price": 315000,
      "Buyer's Agent": "Fred Flintstone",
      "Submitted Date": "2026-06-24",
      "Status": "Pending Review",
      "Seller Concessions": 10000,
      "Buyer Due Diligence Deadline": "2026-07-08",
      "Settlement Date": "2026-07-24",
      "Notes": "THIS IS A TEST!!!   ",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ],
      "Agent Email": "fredflintstonebr@outlook.com",
      "Agent Phone": "(801) 801-8001",
      "Buyer Name(s)": "Barney Rubble",
      "Financing Type": "Cash",
      "Earnest Money Deposit": 10000,
      "Offer PDF URL": "https://www.jotform.com/inbox/6580919375241673545",
      "Listing Address": "8278 S RESACA DR #L1, Sandy, UT 84070",
      "Home Warranty": "Yes"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
recnYuAl33QJIUt4q
rec6eru7ntdBRJE05
Update Offers records
To update Offers records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Offers records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Notes may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

Values for Accept Offer and Street Address are automatically computed by Airtable and cannot be directly updated. You cannot clear these, even with a replace call.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Offers').update([
  {
    "id": "recnYuAl33QJIUt4q",
    "fields": {
      "Offer Name": "TEST — Offer #2 (Chen)",
      "Offer Price": 358000,
      "Buyer's Agent": "Mike Chen",
      "Submitted Date": "2026-06-14",
      "Status": "Declined",
      "Notes": "TEST RECORD — delete after review. FHA offer, below ask, countered — buyer walked.",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ],
      "Agent Email": "mchen@remax.com",
      "Agent Phone": "801-555-0447",
      "Agent Brokerage": "RE/MAX",
      "Buyer Name(s)": "James & Karen Chen",
      "Purchase Type": "Owner Occupied",
      "Financing Type": "FHA",
      "Earnest Money Deposit": 3500,
      "Proof of Funds": true
    }
  },
  {
    "id": "rec6eru7ntdBRJE05",
    "fields": {
      "Offer Price": 315000,
      "Buyer's Agent": "Fred Flintstone",
      "Submitted Date": "2026-06-24",
      "Status": "Pending Review",
      "Seller Concessions": 10000,
      "Buyer Due Diligence Deadline": "2026-07-08",
      "Settlement Date": "2026-07-24",
      "Notes": "THIS IS A TEST!!!   ",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ],
      "Agent Email": "fredflintstonebr@outlook.com",
      "Agent Phone": "(801) 801-8001",
      "Buyer Name(s)": "Barney Rubble",
      "Financing Type": "Cash",
      "Earnest Money Deposit": 10000,
      "Offer PDF URL": "https://www.jotform.com/inbox/6580919375241673545",
      "Listing Address": "8278 S RESACA DR #L1, Sandy, UT 84070",
      "Home Warranty": "Yes"
    }
  },
  {
    "id": "recaBseMhjqjEa34v",
    "fields": {
      "Offer Price": 305000,
      "Buyer's Agent": "Billy Dean",
      "Submitted Date": "2026-06-24",
      "Status": "Pending Review",
      "Seller Concessions": 5000,
      "Buyer Due Diligence Deadline": "2026-07-10",
      "Settlement Date": "2026-07-24",
      "Notes": "Blah blah",
      "Listing": [
        "reckLQ0OlLD3B8sxl"
      ],
      "Agent Email": "bjallen13@hotmail.com",
      "Agent Phone": "(801) 801-8001",
      "Buyer Name(s)": "Billy Mac",
      "Financing Type": "FHA",
      "Earnest Money Deposit": 3000,
      "Offer PDF URL": "https://www.jotform.com/inbox/6580940325245719919",
      "Listing Address": "8278 S RESACA DR #L1, Sandy, UT 84070",
      "Home Warranty": "Yes"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Listing'));
  });
});
Output
[
    "reckLQ0OlLD3B8sxl"
]
[
    "reckLQ0OlLD3B8sxl"
]
[
    "reckLQ0OlLD3B8sxl"
]
Delete Offers records
To delete Offers records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Offers').destroy(['recnYuAl33QJIUt4q', 'rec6eru7ntdBRJE05'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Co-Seller Access Requests Table
The id for Co-Seller Access Requests is tblf0VYMryBefkOuG. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Co-Seller Access Requests table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Addressfld2cMJ9GAmWdX2TyText
string
A single line of text.
 
Example values
"foo", "bar"

How Many Co-SellersfldCOnQaG3QGVTHUfNumber
number
An integer (whole number, e.g. 1, 32, 99). This field only allows positive numbers.
 
Example value
42

Co-Seller 1 NamefldepHwc2MiZdN0J6Text
string
A single line of text.
 
Example values
"foo", "bar"

Co-Seller 1 EmailfldcoxD4jNmxJNSomEmail
string
A valid email address.
 
Example values
"support@example.com", "sales@example.com"

Co-Seller 1 PhonefldlooOKg69l3F6FK
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"(415) 555-9876"

Co-Seller 1 RelationshipfldK4kkKaJcqew2WfText
string
A single line of text.
 
Example values
"foo", "bar"

Co-Seller 2 NamefldpOYkxayZe9A81xText
string
A single line of text.
 
Example values
"foo", "bar"

Co-Seller 2 EmailfldRVK9OO5jwonUfREmail
string
A valid email address.
 
Example values
"support@example.com", "sales@example.com"

Co-Seller 2 PhonefldtwgOrWPC7QxA2n
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"(415) 555-9876"

Co-Seller 2 RelationshipfldBxbToxmeLM92mVText
string
A single line of text.
 
Example values
"foo", "bar"

Co-Seller 3 NamefldoignpThhkzsIpjText
string
A single line of text.
 
Example values
"foo", "bar"

Co-Seller 3 EmailfldSz9j3QL5o1tXx8Email
string
A valid email address.
 
Example values
"support@example.com", "sales@example.com"

Co-Seller 3 PhonefldtzAiwCGRrrMvs9
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"(415) 555-9876"

Co-Seller 3 RelationshipfldDXTWmr66wgxicoText
string
A single line of text.
 
Example values
"foo", "bar"

Welcome Emails Sentfldet2FbeRKMJyHi5
Checkbox
boolean
This field is "true" when checked and otherwise empty.
 
Example value
true

Submitted AtfldI6lNeJv5twD9ai
Date and time
string (ISO 8601 formatted date)
UTC date and time, e.g. "2014-09-05T07:00:00.000Z".
 
Example value
"2014-09-05T07:00:00.000Z"

List Co-Seller Access Requests records
To list records in Co-Seller Access Requests, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Address and How Many Co-Sellers, pass in:

fields: ["Address", "How Many Co-Sellers"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fld2cMJ9GAmWdX2Ty", "fldCOnQaG3QGVTHUf"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Address isn't empty, pass in NOT({Address} = '') as a parameter like this:

filterByFormula: "NOT({Address} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Address in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Address
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Address in descending order, pass in:

[{field: "Address", direction: "desc"}]
view
string
optional
The name or ID of a view in the Co-Seller Access Requests table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Co-Seller Access Requests').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Address'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
To see an example, please add some non-empty records to Co-Seller Access Requests.
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Co-Seller Access Requests').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Address'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Co-Seller Access Requests').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Co-Seller Access Requests record
To retrieve an existing record in Co-Seller Access Requests table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Example request & response
To see an example, please add some non-empty records to Co-Seller Access Requests.
Create Co-Seller Access Requests records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

When creating date time values in Submitted At, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in. Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Co-Seller Access Requests.
Update Co-Seller Access Requests records
To update Co-Seller Access Requests records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Co-Seller Access Requests records will always fail.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
When updating date time values in Submitted At, ambiguous string inputs like "2020-09-05T07:00:00" and "2020-09-08" will be interpreted according to the time zone of the field if specified, and nonambiguous string inputs with zone offset like "2020-09-05T07:00:00.000Z" and "2020-09-08T00:00:00-07:00" will be interpreted as the underlying timestamp.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Co-Seller Access Requests.
Delete Co-Seller Access Requests records
To delete Co-Seller Access Requests records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. 
Example request & response
To see an example, please add some non-empty records to Co-Seller Access Requests.
Closing Team Table
The id for Closing Team is tblLAcvvjwPRcWa7G. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Closing Team table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
NamefldFak7U5bbv66srcText
string
A single line of text.
 
Example values
"Spencer Steed"

"Erin White"

"Corey Lee Miner"

RolefldG39dLnlRi8X3hsText
string
A single line of text.
 
Example values
"Escrow/Title Officer"

"Manager"

"Transaction Coordinator"

CompanyfldiVxuvwbwAA0bqcText
string
A single line of text.
 
Example values
"Steed Title"

"The Margin Co."

"M2 Solutions"

Phonefld9WViobxQzJej7u
Phone number
string
A telephone number, e.g. "(415) 555-9876".
 
Example values
"(385) 434-3800"

"(801) 554-7483"

"801-505-3004"

EmailfldxMNzxiWl3p7KEnEmail
string
A valid email address.
 
Example values
"spencer@steedtitle.com"

"hello@margin-co.com"

"corey@m2saltlake.com"

WebsitefldpR0YvVINJ2gqBgURL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://steedtitle.com"

"https://margin-co.com"

"http://www.mtwosolutions.com/"

TypefldqQOvxMciaAoEyq
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Escrow Officer",
    "Transaction Coordinator"
]
ListingsfldErD5S2B8uFcDeH
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

Listings 2fld65czWpv7VQBMf6
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

List Closing Team records
To list records in Closing Team, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Name and Role, pass in:

fields: ["Name", "Role"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldFak7U5bbv66src", "fldG39dLnlRi8X3hs"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Name isn't empty, pass in NOT({Name} = '') as a parameter like this:

filterByFormula: "NOT({Name} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Name in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Name
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Name in descending order, pass in:

[{field: "Name", direction: "desc"}]
view
string
optional
The name or ID of a view in the Closing Team table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Closing Team').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Name'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
Retrieved Spencer Steed
Retrieved Erin White
Retrieved Corey Lee Miner
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Closing Team').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Name'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Closing Team').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Closing Team record
To retrieve an existing record in Closing Team table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Closing Team').find('rec39Hafyn2Y9Jlla', function(err, record) {
    if (err) { console.error(err); return; }
    console.log('Retrieved', record.id);
});
Output
Retrieved rec39Hafyn2Y9Jlla
Create Closing Team records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in (click to show example). Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Closing Team').create([
  {
    "fields": {
      "Name": "Spencer Steed",
      "Role": "Escrow/Title Officer",
      "Company": "Steed Title",
      "Phone": "(385) 434-3800",
      "Email": "spencer@steedtitle.com",
      "Website": "https://steedtitle.com",
      "Type": "Escrow Officer",
      "Listings": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "fields": {
      "Name": "Erin White",
      "Role": "Manager",
      "Company": "The Margin Co.",
      "Phone": "(801) 554-7483",
      "Email": "hello@margin-co.com",
      "Website": "https://margin-co.com",
      "Type": "Transaction Coordinator",
      "Listings 2": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function (record) {
    console.log(record.getId());
  });
});
Output
rec39Hafyn2Y9Jlla
recgsIboMs68c7mg2
Update Closing Team records
To update Closing Team records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Closing Team records will always fail. The example at the right uses the non-destructive update method. Click here to show a destructive replace call.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listings and Listings 2, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Closing Team').update([
  {
    "id": "rec39Hafyn2Y9Jlla",
    "fields": {
      "Name": "Spencer Steed",
      "Role": "Escrow/Title Officer",
      "Company": "Steed Title",
      "Phone": "(385) 434-3800",
      "Email": "spencer@steedtitle.com",
      "Website": "https://steedtitle.com",
      "Type": "Escrow Officer",
      "Listings": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recgsIboMs68c7mg2",
    "fields": {
      "Name": "Erin White",
      "Role": "Manager",
      "Company": "The Margin Co.",
      "Phone": "(801) 554-7483",
      "Email": "hello@margin-co.com",
      "Website": "https://margin-co.com",
      "Type": "Transaction Coordinator",
      "Listings 2": [
        "reckLQ0OlLD3B8sxl"
      ]
    }
  },
  {
    "id": "recMtClZZtCnFak08",
    "fields": {
      "Name": "Corey Lee Miner",
      "Role": "Transaction Coordinator",
      "Company": "M2 Solutions",
      "Phone": "801-505-3004",
      "Email": "corey@m2saltlake.com",
      "Website": "http://www.mtwosolutions.com/",
      "Type": "Transaction Coordinator"
    }
  }
], function(err, records) {
  if (err) {
    console.error(err);
    return;
  }
  records.forEach(function(record) {
    console.log(record.get('Phone'));
  });
});
Output
"(385) 434-3800"
"(801) 554-7483"
"801-505-3004"
Delete Closing Team records
To delete Closing Team records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. Click here to show an example.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Closing Team').destroy(['rec39Hafyn2Y9Jlla', 'recgsIboMs68c7mg2'], function(err, deletedRecords) {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Deleted', deletedRecords.length, 'records');
});
Output
Deleted 2 records
Documents Table
The id for Documents is tblx5BL7ocWKXSPdh. Table ids and table names can be used interchangeably in API requests. Using table ids means table name changes do not require modifications to your API request.

Fields
Each record in the Documents table contains the following fields:

Field names and field ids can be used interchangeably. Using field ids means field name changes do not require modifications to your API request. We recommend using field ids over field names where possible, to reduce modifications to your API request if the user changes the field name later.

Field NameField IDTypeDescription
Document NamefldC8K3nYQu3QshESText
string
A single line of text.
 
Example values
"foo", "bar"

Categoryfldb9UvwDec7UtkvS
Single select
string
Selected option name.

When creating or updating records, if the choice string does not exactly match an existing option, the request will fail with an INVALID_MULTIPLE_CHOICE_OPTIONS error unless the typecast parameter is enabled. If typecast is enabled, a new choice will be created if one does not exactly match.

 
Possible values
[
    "Disclosure",
    "Inspection Report",
    "HOA Docs",
    "Contract",
    "Offer",
    "Photos",
    "Other"
]
File URLfldQdSrdKSziOpTOjURL
string
A valid URL (e.g. airtable.com or https://airtable.com/universe).
 
Example values
"https://airtable.com", "http://minesweeperonline.com"

Upload DatefldleEH8teTvoRNB4
Date
string (ISO 8601 formatted date)
UTC date, e.g. "2014-09-05".
 
Example value
"2014-09-05"

Uploaded ByfldvTEJxi2jZ1PAb4Text
string
A single line of text.
 
Example values
"foo", "bar"

Visible to SellerfldPDzuLI6LUeDrJD
Checkbox
boolean
This field is "true" when checked and otherwise empty.
 
Example value
true

NotesfldlRv6CJjykDuUF7Long text
string
Multiple lines of text, which may contain "mention tokens", e.g.
<airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>
 
Example value
"Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque vel accumsan orci. Fusce interdum ipsum nec neque iaculis congue. Vivamus bibendum quam at sollicitudin vestibulum. Nunc suscipit nisl sit amet enim hendrerit pellentesque. Curabitur non fermentum orci."
Listingfld4sbW6w2QAytpyM
Link to another record
array of record IDs (strings)
Array of linked records IDs from the Listings table.
 
Example value
["rec8116cdd76088af", "rec245db9343f55e8", "rec4f3bade67ff565"]

List Documents records
To list records in Documents, use the select method.

select returns a query object. To fetch the records matching that query, use the eachPage or firstPage method of the query object.

Returned records do not include any fields with "empty" values, e.g. "", [], or false.

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

You can use the following parameters to filter, sort, and format the results:

fields
array of strings
optional
Only data for fields whose names are in this list will be included in the result. If you don't need every field, you can use this parameter to reduce the amount of data transferred.

For example, to only return data from Document Name and Category, pass in:

fields: ["Document Name", "Category"]
You can also perform the same action with field ids (they can be found in the fields section):

fields: ["fldC8K3nYQu3QshES", "fldb9UvwDec7UtkvS"]
filterByFormula
string
optional
A formula used to filter records. The formula will be evaluated for each record, and if the result is not 0, false, "", NaN, [], or #Error! the record will be included in the response. We recommend testing your formula in the Formula field UI before using it in your API request.

If combined with the view parameter, only records in that view which satisfy the formula will be returned.

The formula must be encoded first before passing it as a value. You can use this tool to not only encode the formula but also create the entire url you need. For example, to only include records where Document Name isn't empty, pass in NOT({Document Name} = '') as a parameter like this:

filterByFormula: "NOT({Document Name} = '')"

Note: Airtable's API only accepts request with a URL shorter than 16,000 characters. Encoded formulas may cause your requests to exceed this limit. To fix this issue you can instead make a POST request to /v0/{baseId}/{tableIdOrName}/listRecords while passing the parameters within the body of the request instead of the query parameters. See our support article on this for more information.

maxRecords
number
optional
The maximum total number of records that will be returned in your requests. If this value is larger than pageSize (which is 100 by default), you may have to load multiple pages to reach this total. See the Pagination section below for more.pageSize
number
optional
The number of records returned in each request. Must be less than or equal to 100. Default is 100. See the Pagination section below for more.sort
array of objects
optional
A list of sort objects that specifies how the records will be ordered. Each sort object must have a field key specifying the name of the field to sort on, and an optional direction key that is either "asc" or "desc". The default direction is "asc".

The sort parameter overrides the sorting of the view specified in the view parameter. If neither the sort nor the view parameter is included, the order of records is arbitrary.

For example, to sort records by Document Name in descending order, send these two query parameters:

sort%5B0%5D%5Bfield%5D=Document%20Name
sort%5B0%5D%5Bdirection%5D=desc
For example, to sort records by Document Name in descending order, pass in:

[{field: "Document Name", direction: "desc"}]
view
string
optional
The name or ID of a view in the Documents table. If set, only the records in that view will be returned. The records will be sorted according to the order of the view unless the sort parameter is included, which overrides that order. Fields hidden in this view will be returned in the results. To only return a subset of fields, use the fields parameter.cellFormat
string
optional
The format that should be used for cell values. Supported values are:

json: cells will be formatted as JSON, depending on the field type.

string: cells will be formatted as user-facing strings, regardless of the field type. The timeZone and userLocale parameters are required when using string as the cellFormat.

Note: You should not rely on the format of these strings, as it is subject to change.
The default is json.

timeZone
string
optional
The time zone that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

userLocale
string
optional
The user locale that should be used to format dates when using string as the cellFormat. This parameter is required when using string as the cellFormat.

returnFieldsByFieldId
boolean
optional
An optional boolean value that lets you return field objects where the key is the field id.

This defaults to false, which returns field objects where the key is the field name.

recordMetadata
array of strings
optional
An optional field that, if includes commentCount, adds a commentCount read only property on each record returned.

Pagination
The server returns one page of records at a time. Each page will contain pageSize records, which is 100 by default.

To fetch the next page of records, call fetchNextPage.

Pagination will stop when you've reached the end of your table. If the maxRecords parameter is passed, pagination will stop once you've reached this maximum.

 
Code
var Airtable = require('airtable');
var base = new Airtable({apiKey: 'YOUR_SECRET_API_TOKEN'}).base('appNh6k6ZP0c2OtbQ');

base('Documents').select({
    // Selecting the first 3 records in Grid view:
    maxRecords: 3,
    view: "Grid view"
}).eachPage(function page(records, fetchNextPage) {
    // This function (`page`) will get called for each page of records.

    records.forEach(function(record) {
        console.log('Retrieved', record.get('Document Name'));
    });

    // To fetch the next page of records, call `fetchNextPage`.
    // If there are more records, `page` will get called again.
    // If there are no more records, `done` will get called.
    fetchNextPage();

}, function done(err) {
    if (err) { console.error(err); return; }
});
Output
To see an example, please add some non-empty records to Documents.
Fetch first page
// If you only want the first page of records, you can
// use `firstPage` instead of `eachPage`.
base('Documents').select({
    view: 'Grid view'
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved', record.get('Document Name'));
    });
});
Fetch additional record metadata
// If you want to fetch the number of comments for each record,
// include the `recordMetadata` param.
base('Documents').select({
    recordMetadata: ['commentCount']
}).firstPage(function(err, records) {
    if (err) { console.error(err); return; }
    records.forEach(function(record) {
        console.log('Retrieved a record with', record.commentCount, 'comments');
    });
});
 
Iteration may timeout due to client inactivity or server restarts. In that case, the client will receive a 422 response with error message LIST_RECORDS_ITERATOR_NOT_AVAILABLE. It may then restart iteration from the beginning.

 
Retrieve a Documents record
To retrieve an existing record in Documents table, use the find method.

Any "empty" fields (e.g. "", [], or false) in the record will not be returned.

 
Example request & response
To see an example, please add some non-empty records to Documents.
Create Documents records
To create new records, use the create method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first argument should be an array of up to 10 record objects. Each of these objects should have one key whose value is an inner object containing your record's cell values, keyed by either field name or field id.

Returns an array of record objects created if the call succeeded, including record IDs which will uniquely identify the records within Anna — Seller Portal System.

The Airtable API will perform best-effort automatic data conversion from string values if the typecast parameter is passed in. Automatic conversion is disabled by default to ensure data integrity, but it may be helpful for integrating with 3rd party data sources.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Documents.
Update Documents records
To update Documents records, use the update or replace method. An update will only update the fields you include. Fields not included will be unchanged. A replace will perform a destructive update and clear all unincluded cell values. A replace call on Documents records will always fail.
The first argument should be an array of up to 10 record objects. Each of these objects should have an id property representing the record ID and a fields property which contains all of your record's cell values by field name or field id for all of your record's cell values by field name.
To link to new records in Listing, add new linked record IDs to the existing array. Be sure to include all existing linked record IDs that you wish to retain. To unlink records, include the existing array of record IDs, excluding any that you wish to unlink.

Notes may contain "mention tokens". A mention token corresponds to a "@mention" in Airtable's user interface; here in the API it will look like <airtable:mention id="menE1i9oBaGX3DseR">@Alex</airtable:mention>. Mention tokens cannot be created via this API and should be left intact (or wholly removed) when updating long text fields.

Automatic data conversion for update actions can be enabled via typecast parameter. See create record for details.

You can also include a single record object at the top level. 
Example request & response
To see an example, please add some non-empty records to Documents.
Delete Documents records
To delete Documents records, use the destroy method. Note that table names and table ids can be used interchangeably. Using table ids means table name changes do not require modifications to your API request.

The first parameter to destroy is an array of up to 10 record IDs to delete.

You can also set the first parameter to a record ID to delete a single record. 
Example request & response
To see an example, please add some non-empty records to Documents.
Errors
The Anna — Seller Portal System API follows HTTP status code semantics. 2xx codes signify success, 4xx mostly represent user error, 5xx generally correspond to a server error. The error messages will return a JSON-encoded body that contains error and message fields. Those will provide specific error condition and human-readable message to identify what caused the error.

Success code
200OKRequest completed successfully.
User error codes
These errors generally indicate a problem on the client side. If you are getting one of these, check your code and the request details.

400Bad RequestThe request encoding is invalid; the request can't be parsed as a valid JSON.
401UnauthorizedAccessing a protected resource without authorization or with invalid credentials.
402Payment RequiredThe account associated with the API key making requests hits a quota that can be increased by upgrading the Airtable account plan.
403ForbiddenAccessing a protected resource with API credentials that don't have access to that resource.
404Not FoundRoute or resource is not found. This error is returned when the request hits an undefined route, or if the resource doesn't exist (e.g. has been deleted).
413Request Entity Too LargeThe request exceeded the maximum allowed payload size. You shouldn't encounter this under normal use.
422Invalid RequestThe request data is invalid. This includes most of the base-specific validations. You will receive a detailed error message and code pointing to the exact issue.
429Too Many RequestsThe API is limited to 5 requests per second per base. You will receive a 429 status code and a message "Rate limit exceeded. Please try again later" and will need to wait 30 seconds before subsequent requests will succeed. To learn more about rate limits, please visit our Rate Limits guide.
Server error codes
These errors generally represent an error on our side. In the event of a 5xx error code, detailed information about the error will be automatically recorded, and Airtable's developers will be notified.

500Internal Server ErrorThe server encountered an unexpected condition.
502Bad GatewayAirtable's servers are restarting or an unexpected outage is in progress. You should generally not receive this error, and requests are safe to retry.
503Service UnavailableThe server could not process your request in time. The server could be temporarily unavailable, or it could have timed out processing your request. You should retry the request with backoffs.